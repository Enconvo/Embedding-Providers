---
name: embeddings_providers
description: >
  Text embedding providers for semantic search and knowledge base indexing, supporting OpenAI, Voyage AI, Ollama, SiliconFlow, and Enconvo Cloud with configurable concurrency and model selection.
metadata:
  author: EnconvoAI
  version: "1.1.242"
---

## API Reference

Just use the `local_api` tool to request these APIs.

To view full parameter details for a specific endpoint, run: `node skills/scripts/api_detail.cjs <endpoint-path>`

